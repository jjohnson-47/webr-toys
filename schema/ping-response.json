{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ping Response Schema",
  "description": "JSON schema for the /ping endpoint response",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["pong"],
      "description": "Status indicator for the ping response"
    }
  },
  "required": ["status"],
  "additionalProperties": false
}